package org.fage;

import org.fage.cfg.SpringConfig;
import org.springframework.boot.SpringApplication;

public class BootStarp {
	public static void main(String[] args) {
		SpringApplication.run(SpringConfig.class, args);
	}
}
